-- This migration was generated while `candidate_embeddings` was not represented
-- in `schema.prisma`. Keep it as a no-op so fresh databases can replay the
-- migration history without dropping semantic-search storage.
SELECT 1;
